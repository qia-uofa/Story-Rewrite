```env
TARGET=Re-SoP
ALIAS=main
```
```sourceignore
*
!prompt.txt
```
```targetignore
*
!resop.txt
```

Your task is to rewrite the file `resop.txt` using the given prompt `prompt.txt`. 

Write your new story to the file `./resop.txt`.

You must strictly mirror the structure of the original story. 